# Registry provisioning state

This branch is deliberately `unprovisioned`. The contract and validators are
ready, but a production registry cannot be published until the dedicated
licence-status public RSA key, key ID, and fingerprint have been approved and
pinned in every consumer.

Never put a private key or usable licence token in this repository.
